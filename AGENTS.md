# Project Agent Entry Point

本文件是课程要求和智能体自动发现使用的项目上下文入口。完整约束见 [AGENT.md](AGENT.md)，所有智能体在修改代码或文档前必须完整阅读该文件。

## 架构概览

平台数据流为：公开数据源或模拟数据 -> 采集 -> 清洗/去重/消歧 -> 数据库与 Elasticsearch -> 检索/门户/认领/统计服务 -> Web 前端与管理后台。

## 分支约定

- `main` 保存公共规范、统一阶段交付物和最终合并版本。
- `a` 保存 A 组使用模型 A 的候选实现。
- `b` 保存 B 组使用模型 B 的候选实现。
- 两组实现相同核心功能，阶段交付物只形成一套。
- 公共契约变更须经成员 1 和成员 6 共同确认。

## 关键约束

- 先写 SPEC，再生成或修改核心代码。
- 不得单方面修改公共 API、业务实体、数据库语义和验收标准。
- 不得进行无关重构、全项目格式化或未经批准的依赖升级。
- AI 生成代码必须经过人工审查、测试并留下使用记录。
- 数据采集必须遵守版权、隐私和爬虫伦理要求。
- 最终合并应选择一个主版本并逐模块迁移，不直接机械合并两套实现。

## 常用命令

项目技术栈尚未确定，当前没有可验证的构建、测试和启动命令。确定技术栈后应立即补充真实命令；禁止写入未经执行验证的占位命令。
