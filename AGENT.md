# AI 智能体项目注意事项

本文件约束所有参与本项目的 AI 模型和代码智能体。开始任何任务前，必须先阅读 `README.md`、本文件、相关 SRS/SPEC 和公共接口定义。

## 1. 项目目标

本项目实现学术成果分享平台。系统核心数据流为：

```text
公开数据源/模拟数据
    -> 数据采集
    -> 清洗、去重和作者消歧
    -> 数据库与 Elasticsearch
    -> 检索、门户、认领和统计服务
    -> Web 前端与管理后台
```

系统必须支持不少于 10,000 条成果数据，并演示“采集 -> 清洗 -> 入库 -> 检索 -> 统计分析”完整链路。

## 2. 分支边界

- `main`：公共规范、统一交付物、公共契约和最终版本。
- `a`：A 组及模型 A 的候选实现。
- `b`：B 组及模型 B 的候选实现。

执行修改前必须确认当前分支。不得把 A 组实现写入 `b`，也不得把 B 组实现写入 `a`。不得直接在 `main` 中开展未经评审的候选功能开发。

两个分支的核心功能必须相同，但内部架构、算法、UI 和创新功能可以不同。最终合并采用“选择主版本、逐模块迁移”的方式，不机械合并两套完整实现。

## 3. 公共资产

以下内容属于公共资产，任何智能体不得单方面修改：

- SRS、公共 SPEC 和验收标准。
- OpenAPI、错误码、分页格式和认证字段。
- 公共业务实体、ID、枚举、时间和空值规则。
- 数据库基础结构和 Elasticsearch 公共字段语义。
- 公共测试数据、契约测试和端到端验收用例。
- `README.md`、`AGENT.md`、`AGENTS.md` 中的团队规则。

确需修改公共资产时，先输出变更原因、影响范围、兼容方案和迁移步骤，等待成员 1、6 共同确认。

## 4. 规范驱动开发

核心模块编码前必须存在对应 SPEC，至少说明：

1. 功能目标和不在范围内的内容。
2. 输入、输出、接口、数据类型和约束。
3. 正常流程、异常流程和边界条件。
4. 清洗、消歧、检索排序等算法规则。
5. 可执行的验收标准和测试要求。

智能体生成代码前应引用 SPEC 编号。需求或规范不清晰时，应先报告冲突，不得自行扩大功能范围。

## 5. 修改约束

- 只修改当前任务明确涉及的文件。
- 不进行无关重构、批量改名或全项目格式化。
- 不擅自升级依赖、切换框架或引入新的基础设施。
- 优先复用项目已有模式、工具和公共组件。
- 数据库变更必须使用迁移文件，不得直接依赖手工改库。
- 接口变化必须保持向后兼容，或同时提供明确迁移方案。
- 禁止提交密钥、令牌、密码、真实个人信息和未授权数据。
- 长时间任务应拆分步骤；预计超过 1 小时时必须设置人工检查点。

## 6. 数据与爬虫合规

- 优先使用公开数据集、公开 API 或课程模拟数据。
- 不爬取知网等收费数据库、个人隐私或未授权内容。
- 遵守数据源服务条款、robots 规则和合理限速要求。
- 采集记录必须保留来源、时间、版本和处理状态。
- 清洗和消歧结果应可追溯，不得静默丢弃无法判断的数据。
- 模拟数据必须明确标注，不得冒充真实学术数据。

## 7. 测试与质量

每次功能修改至少完成与风险相匹配的测试：

- 数据清洗、消歧、检索排序和统计计算必须有单元测试。
- 登录、检索、认领和审核必须有接口或端到端测试。
- 公共 OpenAPI 必须通过契约测试。
- 错误输入、空数据、重复数据、权限不足和依赖失败必须覆盖。
- 核心模块中至少 3 个模块的行覆盖率不低于 80%。
- 修复缺陷时应先添加能够复现问题的测试。
- 智能体不得通过删除测试或降低断言来制造“测试通过”。

## 8. AI 证据管理

A、B 两组的证据分别保存，但最终汇总为一套课程交付物：

```text
docs/ai/
|-- model-a/
|   |-- prompts/
|   |-- usage-log.md
|   `-- correction-cases.md
`-- model-b/
    |-- prompts/
    |-- usage-log.md
    `-- correction-cases.md
```

每次重要 AI 辅助任务至少记录：

- 任务编号和对应 SPEC。
- 模型名称及版本。
- 提示词或提示词编号。
- 生成结果摘要和修改文件。
- 人工审查与修正内容。
- 测试命令、测试结果和遗留问题。

## 9. 阶段交付物

课程阶段交付物只提交一份。智能体不得为 A、B 两组分别生成两套正式的需求、架构、设计或测试报告。

两组差异写入同一文档的方案 A、方案 B 或附录。各成员的阶段责任、汇编负责人和复核负责人以 `双组并行开发与合并方案.md` 为准。

## 10. 命令与环境

项目技术栈尚未确定，因此当前没有统一的构建、测试和启动命令。技术栈确定后，必须在本节和 `README.md` 中补充真实、可执行的命令。

智能体不得猜测并写入未验证的命令。执行命令后应报告实际结果，不能只根据代码推断成功。

## 11. 完成标准

一项任务只有同时满足以下条件才算完成：

1. 实现与对应 SPEC 一致。
2. 代码可运行，且相关测试通过。
3. 公共契约未被破坏。
4. 文档、接口或模型已同步更新。
5. AI 使用和人工修正证据已记录。
6. 不包含敏感信息、未授权数据或无关改动。
